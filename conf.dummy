#ifndef CONF_HPP
#define CONF_HPP

#define RANDOM_PREFIX_SIZE $random_prefix_size
#define NB_DOMAINS $nb_domains

std::string DOMAINS[NB_DOMAINS] = {
 $domains
};

std::string MYSQL_USER = "$mysql_user";
std::string MYSQL_PASSWD = "$mysql_passwd";
std::string MYSQL_ADDRESS = "$mysql_address";
std::string MYSQL_DB = "$mysql_db";
#ifndef MYSQL_PORT //Usually def in libmysql
#define MYSQL_PORT = $mysql_port;
#endif

#endif
